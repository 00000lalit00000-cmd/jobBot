Setup project & environment

Goal: Create project folder, virtualenv, install dependencies, and configure environment variables.

Inputs:
- Python installed

Outputs:
- `requirements.txt`, `.env.example`, and instructions to create venv
- Acceptance criteria: Working virtualenv and ability to run `send_test.py`

Steps:
1. Create folder structure and venv.
2. Add `requirements.txt` and `.env.example`.
3. Provide commands to activate venv and install deps.
