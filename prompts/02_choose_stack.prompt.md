Choose stack & architecture

Goal: Select language, libraries, storage, and orchestration tools for the job-notification app.

Inputs:
- Constraints (you have Python installed; prefer simple deploy)

Outputs:
- Chosen stack and short justification
- Acceptance criteria: Clear list of packages and orchestration choice (APScheduler or n8n)

Steps:
1. Recommend Python + APScheduler + SQLite for minimal stack.
2. Optionally propose n8n for no-code flows or Docker for deployment.
3. Provide command list to install core packages.
